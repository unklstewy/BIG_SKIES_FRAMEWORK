# configs/

This directory contains configuration files for the BIG SKIES Framework.

Files:
- `config.example.yaml` - Example configuration template
- `mqtt.yaml` - MQTT broker configuration
- `postgres.yaml` - Database configuration
- `security.yaml` - Security and mTLS configuration
