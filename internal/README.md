# internal/

This directory contains private application code that should not be imported by external projects.

Structure:
- `internal/coordinators/` - Coordinator implementations
- `internal/engines/` - Engine implementations (health check, security, etc.)
- `internal/services/` - Service implementations
- `internal/models/` - Internal data models
- `internal/config/` - Configuration management
